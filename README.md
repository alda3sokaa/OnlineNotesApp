# OnlineNotesApp
## Contributors

Sara Mahfouz: Developed the backend Data Layer (`NoteRepository`) leveraging Spring Data JPA for H2 database integration, and engineered the RESTful APIs (`NoteController`) to handle core CRUD operations.

